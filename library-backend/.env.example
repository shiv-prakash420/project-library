# Example .env for library-backend
# Copy this file to .env and replace the values before running the backend.
# Example Postgres connection (replace USER, PASSWORD, HOST, PORT, DBNAME):
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE_NAME?schema=public"

# Optional: set NODE_ENV or other env vars used by the app
# NODE_ENV=development